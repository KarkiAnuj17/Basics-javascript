let obj={
  name:"pen",
  price:200,
  rating:4
}
let output=`object name is ${obj.name} which price is ${obj.price} and rating is ${obj.rating}`;
console.log(output)

//providing with the new username startinf with @ ,fulname and length of fullname 
let fullname=prompt("Enter you name");
let newName ="@" + fullname + fullname.length;
console.log(newName);